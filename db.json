[
    {
      "model": "timeline_editor_api.waveform",
      "pk": 1,
      "fields": {
        "name": "Bump",
        "definition": "function definition(time, duration) { var t = time / duration; return Math.pow(Math.sin(Math.PI * t), 2); }"
      }
    }
  ]
